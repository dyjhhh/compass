# compass
Static, **AES-256 client-side encrypted** dashboards (StatiCrypt). This public repo contains only ciphertext — no readable content without the password. Plaintext source lives in a separate private repo. Hosted via GitHub Pages.
